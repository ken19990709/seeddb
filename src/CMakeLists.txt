# SeedDB source library
add_subdirectory(common)
add_subdirectory(parser)
add_subdirectory(storage)
# add_subdirectory(executor)  # Disabled: pending migration to new StorageManager API
# add_subdirectory(cli)       # Disabled: pending migration to new StorageManager API
